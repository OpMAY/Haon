<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mapper.BundleTracesMapper">
    <resultMap id="Trace" type="com.model.farm.trace.Trace">
        <result property="entity" column="entity" typeHandler="JsonObjectTypeHandler"/>
        <result property="breed" column="breed" typeHandler="JsonArrayObjectTypeHandler"/>
        <result property="butchery" column="butchery" typeHandler="JsonArrayObjectTypeHandler"/>
        <result property="process" column="process" typeHandler="JsonArrayObjectTypeHandler"/>
    </resultMap>
    <select id="getBundleTraces" resultType="Trace" resultMap="Trace">
        select *
        from trace
        WHERE no in (select trace_no from bundle_traces WHERE bundle_no = #{bundle_no})
    </select>
</mapper>